// its a library so it need to be made avaiable at user level


/*
* The marco below
* prevents multiple inclustions
*/
#ifndef __VFS_EXLIB_H__   // if not defined 
#define __VFS_EXLIB_H__ //then define 
int printmsg(int val); //print msg

#endif //end if message

